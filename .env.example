# Copia este archivo como .env y rellena los valores
# En Railway estas variables se configuran en el dashboard, no en el archivo

# Puerto en el que corre el servidor (Railway lo asigna automáticamente)
PORT=3000

# En producción, pon la URL de tu app en Railway (ej: https://artline.up.railway.app)
# En desarrollo déjalo vacío o pon http://localhost:3000
FRONTEND_URL=
